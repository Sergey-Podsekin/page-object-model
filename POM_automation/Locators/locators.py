class Locators():

    # Login page objects
    email_textbox_id = "email"
    password_textbox_id = "passwd"
    login_button_xpath = '//*[@class="icon-lock left"]'
    auth_failed_message = "// *[contains(text(), 'Authentication failed.')]"

    # MyAccount page objects
    sign_out_button_xpath = '//*[@class="logout"]'
